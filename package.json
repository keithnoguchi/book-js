{
	"name": "book-js",
	"version": "0.0.2",
	"license": "MIT",
	"private": true,
	"workspaces": [
		"ch02"
	],
	"scripts": {
		"dev": "node",
		"lint": "prettier --plugin-search-dir . --check . && eslint .",
		"fmt": "prettier --plugin-search-dir . --write ."
	},
	"devDependencies": {
		"prettier": "^2.8.7"
	}
}
