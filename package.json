{
	"name": "book-js",
	"version": "0.0.2",
	"license": "MIT",
	"private": true,
	"workspaces": [
		"ch02"
	],
	"scripts": {
		"check": "prettier --plugin-search-dir . --check . && eslint .",
		"dev": "node",
		"fmt": "prettier --plugin-search-dir . --write ."
	},
	"devDependencies": {
		"prettier": "^2.8.7"
	}
}
